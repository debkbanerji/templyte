{
    "rules": {
        "template-directory": {
            ".read": "auth !== null",
            "$uid":{
                ".write": "auth !== null && !data.exists()"
            }
        },
        "template-render-info": {
            ".read": "auth !== null",
            "$uid":{
                ".write": "auth !== null && !data.exists()"
            }
        }
    }
}